# itam-planner API

This package owns the public academic data ingestion, normalization, SQLite persistence, and JSON export pipeline for `itam-planner`.

The package is intentionally backend and data focused. It does not implement the web onboarding flow or other deferred UI slices.
